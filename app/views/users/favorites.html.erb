<h3 class="text-center">My気になる！</h3>

<div class="row">
  <div class="offset-2 col-8">
    <% if @favorite_posts.empty? %>
      <p>まだ「気になる！」レビューがありません</p>
    <% else %>
      <% @favorite_posts.each do |post| %>
        <%= render 'posts/post', post: post %>
      <% end %>
    <% end %>
  </div>
</div>