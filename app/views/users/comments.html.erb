<h3>コメントしたレビュー一覧</h3>

<% if @comment_posts.empty? %>
  <p>まだコメントしたレビューがありません</p>
<% else %>
  <% @comment_posts.each do |post| %>
    <%= link_to post_path(post.id) do %>
      <%= attachment_image_tag post, :image, :fill, 200, 200 %>
      <%= post.item_name %>
    <% end %>
    <%= post.user.name %>
  <% end %>
<% end %>