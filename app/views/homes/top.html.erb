<div id="box">
  <div class="text-center top-text">
    <h2><strong>「AJIHEN」は調味料に焦点を当てた情報共有のためのレビューサイトです</strong></h2>
    <div><strong>詳しく</strong>は<%= link_to "こちら", about_path %></div>
  </div>
  <div class="top-back"></div>
  <div id="images">
    <div class="image1"></div>
    <div class="image2"></div>
    <div class="image3"></div>
    <div class="image4"></div>
  </div>
</div>

<%= render 'searches/form' %>

<div class="row">
  <div class="offset-lg-1 col-lg-10">
    <h2 class="text-center">新着レビュー</h2>
    <div class="row top-post" style="justify-content: center;">
      <%= render 'posts/post' %>
    </div>
    <div class="text-center"><%= link_to "全ての投稿を見る＞", posts_path, class: "link", style: "text-decoration: underline;" %></div>
  </div>
</div>

<div class="row">
  <div class="offset-lg-1 col-lg-7">
    <div class="text-center my-5">
      <h3>カテゴリ一覧</h3>
      <% @categories.each do |category| %>
        <%= link_to category.name, category_search_path(category_id: category.id), class: "link", style: "text-decoration: underline;" %> |
      <% end %>
    </div>
    <div class="text-center">
      <h3>タグ一覧</h3>
      <% @tags.each do |tag| %>
        <%= link_to tag.name, tag_search_path(tag_id: tag.id), class: "link", style: "text-decoration: underline;" %> |
      <% end %>
    </div>
  </div>

  <div class="col-lg-3 mt-5">
    <%= render 'posts/rank', week_ranks: @week_ranks, month_ranks: @month_ranks %>
  </div>
</div>

<%= render "posts/new_link" %>