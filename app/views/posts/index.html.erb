<%= render 'searches/form' %>

<%= render "posts/new_link" %>

<div class="row">
  <div class="col-lg-9">

    <h2 class="text-center">みんなの投稿</h2>

    <div class="btn-group offset-1">
      <button class="sort-title dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        並び替え
      </button>
      <div class="dropdown-menu sort-item">
        <div><%= link_to "新着順", posts_path(sort_create: "true"), class: "sort-link" %></div>
        <div><%= link_to "オススメ度順", posts_path(sort_rate: "true"), class: "sort-link" %></div>
        <div><%= link_to "コメント多い順", posts_path(sort_comment: "true"), class: "sort-link" %></div>
        <div><%= link_to "気になる！順", posts_path(sort_favorite: "true"), class: "sort-link" %></div>
        <div><%= link_to "高スコア順", posts_path(sort_high: "true"), class: "sort-link" %></div>
      </div>
    </div>

    <details class="score-box">
    　<summary class="offset-1 score-title">スコアとは？</summary>
        <div class="offset-1 alert alert-success">
          スコアはレビュー内容を分析しポジティブな内容であるほど高いスコアを示します。<br>
          0.1~1の間の場合表示され、それ以下のスコアは表示されません。
        </div>
    </details>

    <div class="row" style="justify-content: center;" id="post-list">
      <%= render 'posts/post', posts: @posts %>
    </div>
    <div class="row mb-4" style="justify-content: center;">
      <%= link_to_next_page @posts, "もっと見る", remote: true, id: "view_more", class: "more" %>
    </div>
  </div>

  <div class="col-lg-3 mt-5">
    <%= render 'posts/rank', week_ranks: @week_ranks, month_ranks: @month_ranks %>
  </div>
</div>