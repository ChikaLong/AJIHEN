<div class="offset-1 col-10 mt-4 mb-5">
  <%= form_with url: word_search_path, method: :get, local: true do |f| %>
    <div class="input-group">
      <%= f.text_field :keyword, placeholder: "キーワードでレビューを検索", class: "form-control" %>
      <span class="input-group-append">
        <%= button_tag type: 'submit', class: "btn btn-outline-secondary" do %>
          <i class="fas fa-search"></i>
        <% end %>
      </span>
    </div>
  <% end %>
</div>
